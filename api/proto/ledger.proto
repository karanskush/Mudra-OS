syntax = "proto3";

package fintech.ledger.v1;

option go_package = "fintech-api/proto/gen/ledger";

import "google/protobuf/timestamp.proto";
import "common.proto";

// LedgerService service definition
service LedgerService {
  // Create a new GL account (asset, liability, revenue, etc.)
  rpc CreateAccount(CreateAccountRequest) returns (CreateAccountResponse);
  
  // Fetch static metadata for an account
  rpc GetAccount(GetAccountRequest) returns (GetAccountResponse);
  
  // Retrieve point-in-time balance for reconciliation and reporting
  rpc Balance(BalanceRequest) returns (BalanceResponse);
}

// Account message
message Account {
  string account_id = 1;
  string user_id = 2;
  string account_number = 3;
  string name = 4;
  string description = 5;
  string currency = 6;
  fintech.common.v1.AccountType type = 7;
  fintech.common.v1.AccountStatus status = 8;
  string parent_id = 9;  // Optional parent account ID
  bool is_system = 10;
  google.protobuf.Timestamp created_at = 11;
  google.protobuf.Timestamp updated_at = 12;
}

// Balance information
message Balance {
  string account_id = 1;
  double balance = 2;
  string currency = 3;
  google.protobuf.Timestamp as_of = 4;
}

// Request/Response messages
message CreateAccountRequest {
  string user_id = 1;
  string account_number = 2;
  string name = 3;
  string description = 4;
  string currency = 5;
  fintech.common.v1.AccountType type = 6;
  string parent_id = 7;  // Optional
}

message CreateAccountResponse {
  Account account = 1;
  string message = 2;
}

message GetAccountRequest {
  string account_id = 1;
}

message GetAccountResponse {
  Account account = 1;
}

message BalanceRequest {
  string account_id = 1;
  google.protobuf.Timestamp at = 2;  // Optional point-in-time
}

message BalanceResponse {
  Balance balance = 1;
} 